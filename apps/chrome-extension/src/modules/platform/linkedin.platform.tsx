import { Button } from "@mantine/core";

export default function LinkedinPlatform() {
  return (
    <>
      <div>Apply on Linkedin</div>
      <Button onClick={() => {}} type="button">
        Get page data
      </Button>
      <Button onClick={() => {}} type="button">
        Apply to job
      </Button>
      <Button onClick={() => {}} type="button">
        Auto apply
      </Button>
    </>
  );
}
